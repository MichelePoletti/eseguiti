# Unione contabili

Strumento interno per unire le contabili di un cliente in un unico PDF (< 2 MB).

- Funziona interamente nel browser: **nessun file viene caricato su server**.
- L'accesso è protetto da password (la pagina è cifrata).
- Nessun dato di cliente è contenuto in questo repository.
